using System;
using System.Collections;
using System.Collections.Generic;
using Fungus;
using UnityEngine;
//using DG.Tweening;

public class DoTweenFade : MonoBehaviour
{
    /*private Renderer objectRenderer;
    private Color originalColor;
    public Color targetcolor;

    // Start is called before the first frame update
    void Start()
    {
        //get renderer component from gameobj
        objectRenderer = GetComponent<Renderer>();

        //store the original color (including alpha)
        originalColor = objectRenderer.material.color;

        //set the target color with the same RGB values as the original but set alpha to 0
        targetcolor = new Color(originalColor.r, originalColor.g, originalColor.b, 0f);
    }

    void FadeIn(float duration)
    {
        //use doTween to gradually change the alpha
        objectRenderer.material.DOColor(originalColor, duration);
        
    }*/

    /*void FadeOut(float duration)
    {
        //use doTween to gradually change the alpha
        objectRenderer.material.DOColor(targetcolor, duration);
        
    }*/
    

    // Update is called once per frame
    void Update()
    {
        
    }
}
